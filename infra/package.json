{
  "name": "@seismic-sentry/infra",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "seismic-sentry": "dist/bin/app.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "cdk": "cdk",
    "synth": "npm run build && cdk synth",
    "deploy": "npm run build && cdk deploy",
    "destroy": "cdk destroy"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.169.0",
    "constructs": "^10.4.2",
    "source-map-support": "^0.5.21"
  },
  "devDependencies": {
    "aws-cdk": "^2.169.0",
    "typescript": "^5.6.3"
  }
}
